#ifndef INSERTION_SORT_UNIQUE
#define INSERTION_SORT_UNIQUE
#include <iostream>
#include <memory>

void insertion_sort_unique(std::unique_ptr<int[]>&, size_t&);
void input_data_unique(std::unique_ptr<int[]>&, size_t&);
void output_unique(std::unique_ptr<int[]>&, size_t&);

#endif
