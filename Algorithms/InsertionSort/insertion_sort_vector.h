#ifndef INSERTION_SORT_VECTOR
#define INSERTION_SORT_VECTOR
#include <vector>
#include <iostream>

void insertion_sort_vector(std::vector<int>&, size_t&);
void input_data_vector(std::vector<int>&, size_t&);
void output_vector(std::vector<int>&, size_t&);

#endif
